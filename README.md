# CipherChain
CipherChain is a lightweight, cryptographically secure blockchain implementation in Python. It features a custom Merkle Tree architecture for efficient data integrity verification, automated blockchain validity checking, and a simulation suite for detecting unauthorized data tampering.
